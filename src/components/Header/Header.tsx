import { ScrollProgressBar } from '@components/ScrollProgressBar'
import { Navbar } from '@components/Navbar'

export const Header = () => {
  return (
    <header>
      <ScrollProgressBar />
      <Navbar />
    </header>
  )
}
